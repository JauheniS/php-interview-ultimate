---
title: "47. What is the difference between `WHERE` and `HAVING`?"
---

# 47. What is the difference between `WHERE` and `HAVING`?

**Answer:** `WHERE` filters rows *before* aggregation, while `HAVING` filters *after* aggregation (usually with `GROUP BY`).

---

:::info
Category: MySQL & Databases | Level: Junior
:::

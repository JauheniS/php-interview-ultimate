---
title: "49. What is the difference between `CHAR` and `VARCHAR`?"
---

# 49. What is the difference between `CHAR` and `VARCHAR`?

**Answer:** `CHAR` is fixed-length (padded with spaces), while `VARCHAR` is variable-length (uses only the necessary space plus 1-2 bytes for length).

---

:::info
Category: MySQL & Databases | Level: Junior
:::
